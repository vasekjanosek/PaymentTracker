You can run application from terminal.
Application can be build and compiled without any arguments.
File with payments can be given as argument in format '-f path to file'.
To end application write quit.
To add payment write CODE and AMOUNT - code must have 3 uppercase characters.